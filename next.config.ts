import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['edinamag.com','essentialsaltspa.com', 'images.unsplash.com', 's.alicdn.com'], // Add actual domains used
  },
};

export default nextConfig;
